-- ============================================================================
-- 가평: 반려견(댕댕이) 입장 수 컬럼 추가
-- ============================================================================
-- reservations 에 dog_count 컬럼 추가 (예약별 댕댕이 마릿수).
-- 댕댕이 운동장&풀장 이용권은 관리자가 [이용권 관리]에서 직접 등록한다.
-- ============================================================================

ALTER TABLE public.reservations
  ADD COLUMN IF NOT EXISTS dog_count INTEGER NOT NULL DEFAULT 0;
