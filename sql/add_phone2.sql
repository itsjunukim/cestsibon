-- ============================================================================
-- 예약: 추가 전화번호(phone2) 컬럼
-- ============================================================================
-- 예약자와 실제 방문자가 다른 경우, 방문자(또는 보조) 연락처를 별도로 저장한다.
-- 기존 phone 은 그대로 '예약자 연락처'로 사용한다.
-- ============================================================================

ALTER TABLE public.reservations
  ADD COLUMN IF NOT EXISTS phone2 TEXT;
